name: discover narratives from phone

on:
  issues:
    types:
      - opened
      - edited
      - reopened

permissions:
  contents: read
  issues: write

concurrency:
  group: narrative-radar-discovery-issue-${{ github.event.issue.number }}
  cancel-in-progress: true

jobs:
  discover:
    if: >-
      github.event.issue.user.login == github.repository_owner &&
      startsWith(github.event.issue.title, '[RADAR DISCOVERY]')
    runs-on: ubuntu-latest
    env:
      TAVILY_API_KEY: ${{ secrets.TAVILY_API_KEY }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Restore discovery history
        uses: actions/cache@v4
        with:
          path: data/narrative_radar.db
          key: narrative-radar-discovery-db-${{ github.run_id }}
          restore-keys: narrative-radar-discovery-db-
      - name: Install dependencies
        run: python -m pip install -r requirements.txt
      - name: Run owner-requested discovery
        continue-on-error: true
        run: >-
          python -m app.issue_discovery_main
          --event "$GITHUB_EVENT_PATH"
          --json-output narrative-discovery.json
          --markdown-output narrative-discovery.md
          --notification-output discovery-notification.json
      - name: Reply with discovery research
        if: ${{ always() && hashFiles('narrative-discovery.md') != '' }}
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const marker = '<!-- narrative-radar-discovery-report -->';
            const body = fs.readFileSync('narrative-discovery.md', 'utf8');
            const comments = await github.paginate(
              github.rest.issues.listComments,
              {
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                per_page: 100,
              }
            );
            const existing = comments.find(
              (comment) => comment.user?.type === 'Bot' && comment.body?.includes(marker)
            );
            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body,
              });
            }
      - name: Upload full discovery report
        if: ${{ always() && hashFiles('narrative-discovery.json') != '' }}
        uses: actions/upload-artifact@v4
        with:
          name: narrative-radar-discovery-issue-${{ github.event.issue.number }}
          path: narrative-discovery.json
